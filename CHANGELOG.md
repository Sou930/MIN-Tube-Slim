# 更新履歴 (開発者向け)

このファイルは開発者向けの詳細な変更履歴です。
ユーザー向けの簡潔な紹介は `README.md` を参照してください。
`/changelog` ページおよび `/api/changelog` はこのファイルをパースして表示します。

## 更新履歴
### v1.4.1
**🐛 バグ修正**
- **プレイリスト連続再生でエラー (例: `152 - 5`) が出て再生できない問題を修正**
  - EDU モード用パラメータ取得 (`/scratch-edu`, `/kahoot-edu`) で、外部設定の URL パラメータが HTML エスケープ (`&amp;`) されたまま埋め込まれ、`youtubeeducation` 側でパラメータが壊れて再生エラーになっていたのを修正（エンティティを復元するように変更）。
  - 連続再生ページ (`/playlist-play`) に **複数の埋め込みソースへの自動フォールバック** を実装。
    - 1 つの埋め込み方法でエラーになっても、`YT.Player` / `youtube-nocookie` / `scratch-edu` / `kahoot-edu` を順番に試行して再生を継続。
    - `YT.Player` の `onError` と再生開始監視タイマーで再生失敗を検知。
  - すべてのソースで再生できない場合に **「別の方法で再試行 / YouTubeで開く / 次の動画へ」** を選べるエラー画面を表示。
  - `/scratch-edu`・`/kahoot-edu` の外部設定取得が失敗してもサーバーが 500 を返さず、安全なフォールバック URL を返すように改善。

### v1.4.0
**✨ 新機能**
- **プレイリスト連続再生ページ** (`/playlist-play`) を新設
  - 再生リスト内の動画を **自動で次々と連続再生**（YouTube IFrame API で動画終了を検知）
  - 右サイドに動画一覧を表示し、クリックで任意の動画へジャンプ・現在再生中をハイライト
  - **自動再生 / ループ / シャッフル** のトグル（設定は `localStorage` に保存）
  - **EDU モード**（YouTube Education 経由）に切り替えて再生可能
  - 動画終了時に「次の動画」トースト（カウントダウン付き・キャンセル/即再生）
  - ホームの再生リストカード・詳細から「再生」「すべて再生」ボタンで起動
- **更新履歴ページ** (`/changelog`) を新設
  - 更新履歴セクションを自動でパースしてサイト上に表示（API: `/api/changelog`）
  - サイドバーから「更新履歴」リンクでアクセス可能・最新バージョンに NEW バッジ
- コメントの投稿時期を相対時刻で表示
- コメントの返信を見られる機能を追加
- 再生ページから **再生リストに動画を追加** できる「保存」ボタンを追加
  - 既存の再生リストへワンタップで追加 / 解除（トグル）
  - その場で新しい再生リストを作成して追加
  - ホーム画面の再生リスト (`mt_playlists`) と完全互換
  - 追加済みの場合は「保存済み」表示でボタンを強調

**🎨 改善**
- **「すべて（おすすめ）」と関連動画の精度を改善**
  - 元動画のタイトル・チャンネルとの一致度を計算する関連性スコアリングを導入
  - 日本語/英数字対応の簡易トークナイザ（バイグラム分解）＋ストップワード除外で、
    無関係な動画の混入を抑制
  - おすすめフィードは視聴履歴・登録チャンネルから「興味プロファイル」を構築し、
    関連度の高い動画を優先しつつ「発見枠」を少量だけ混ぜる方式に変更
  - 関連動画は ID/類似タイトルの重複除去後にスコア順で並べ替え、
    関連が少ない場合はフォールバックでフィードを補完
- 再生ページのデザインを刷新（アクションバーのホバー・押下アニメーション、ボタンの統一感を向上）
- 再生リスト保存モーダルを新設（ブラー背景・ポップアニメーション・トースト通知）
### v1.3.3
**⚡ 改善**
- 検索バックエンドに **Study2525 (2525)** を並列ソースとして再導入
  - `youtube-search-api` と Study2525 (Invidious 互換) を **同時並列リクエスト**
  - **Promise.race** ベースの戦略で、先に返って来た結果を即座にユーザーへ返却（体感速度の大幅向上）
  - 後発の結果はバックグラウンドでマージし、5 分キャッシュに蓄積（次回アクセス時の精度向上）
  - 片方のバックエンドが落ちていても継続動作する耐障害設計
  - 複数ミラー (`study2525.glitch.me`, `yt.chocolatemoo53.com`) でフェイルオーバー

**🎨 ANIME Viewer のデザインを大幅刷新**
- カラーパレットを「アニメ・ノワール」テーマに刷新（紫→ピンク→オレンジのグラデーション）
- 浮遊する Orb アニメーション・グリッドオーバーレイ・ノイズテクスチャによる没入感のある背景
- ヘッダーをスティッキー化し、スクロール時にシャドウ・透明度を変化させる
- お気に入り / 履歴をモーダルではなく **タブビュー** に変更（件数バッジ付き）
- ジャンルチップ（人気・新作・バトル・ラブコメ・ファンタジー・SF・日常・スポーツ）でワンタップ検索
- カードに「♥お気に入り済み」バッジ・グラデーションボーダーを追加
- 検索履歴を 10 件まで保存・クリアボタン付きドロップダウン
- 視聴履歴を 30 件まで拡張、相対時刻表示（◯分前 / ◯時間前 / ◯日前）
- モバイル UI を全面的に再設計（小型端末でも 2 カラムで快適）
- `prefers-reduced-motion` 対応で動作軽減モードに配慮

### v1.3.2
- APIレスポンスキャッシュを改善
- スケルトンUIを改善
- フィルター位置をYouTube準拠に改善
### v1.3.1
**改善点**
- モバイル UI の改善

**以下の問題を修正**
- 「新しい再生リストを作成」ボタンの文字が見にくい問題
- チャンネルアイコンの上にイニシャル 1 文字が表示されるバグ
### v1.3.0
**✨ 新機能**
- 登録チャンネルの新着動画通知システムを追加
- 自動で次の動画を再生する機能を追加
- シアターモードを追加
- 検索フィルターを追加
- 関連ハッシュタグ表示に対応
- ダウンロードリンク生成機能を追加
- ジャンルやキーワードから自動でプレイリストを作成する機能を追加

**⚡ 改善**
- 読み込み速度を改善
- API機能を強化
- デザインを改善
- ローディング画面（土星で「Mino」と表示される画面）を削除

**🧹 削除・整理**
- 再生方法の選択肢から「2525」と「K-Tube」を削除
- Elixirプロキシを削除
- SystemCheckを削除
- Abyssを削除

以下のゲームを削除
- 人狼
- Deep Miner
- SnowBall.io
- SnowRide
- Terraria
- 2048 (PC)
- Undertale (Sans Fight)
- 「ANIME」と「Game」カテゴリーを統合
- Ktubeを削除
### ver1.2.0
- 動画一覧ページにチャンネルの画像が表示されずに簡易表示されてしまうバグを修正
- MinTube での偽装ページ / ローディング表示 (humanVerified 確認画面) を削除
- 検索結果にチャンネルとプレイリストを表示するよう追加
- Elixirプロキシを削除
- 読み込みを最大限高速化
### ver1.1.0
- 12アプリを削除
### ver1.0.0
MinTubeの派生プロジェクトとしてSlimが開始
